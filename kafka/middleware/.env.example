cat > .env.example << 'EOF'
# Porta do servidor HTTP
PORT=8081

# Brokers Kafka (separados por vírgula, SEM ESPAÇOS)
KAFKA_BROKERS=kafka-broker-1:9092,kafka-broker-2:9092,kafka-broker-3:9094

# Nível de log (info, debug, error)
LOG_LEVEL=info

# Observações:
# - NÃO coloque informações sensíveis neste arquivo
# - Copie para .env e ajuste conforme seu ambiente
# - O arquivo .env NÃO deve ser commitado no Git
EOF
